# 阶段7交付说明

版本：`0.7.0` · 阶段：`7` · 存档版本：`6`

## 本阶段完成

- 新增 `js/unit-status.js`，统一 `intact / light / heavy / destroyed` 损伤判定；维修、生产、编队、战区和存档统一使用。
- 维修任务保存规范化为配置来源的 `costPaid`，修复成本篡改；返回中编队和非法单位状态返回明确错误码。
- 战报增加严格元数据、友军派遣快照、生命值快照、完整时间轴、结果事件和精确奖励校验；历史战报写入深拷贝。
- 活动战斗损坏或缺少结算回执时安全关闭并恢复编队/单位，不伪造结算结果；活动战斗离线期间保持暂停。
- 新增技术实验室建筑、Canvas 设备表现、科研分页和九项科技：工业/军备/指挥三个分支各三项。
- 科研支持前置条件、资源扣费、单个当前任务、最多三项等待队列、当前/等待取消退款、暂停、完成效果、存档容错和生产/维修任务研究快照。
- 离线结算支持科研推进、科研完成报告、60秒报告阈值、短离线静默、离线令牌防重放；离线只推进世界时间，不增加累计游玩时间。
- 增加 `window.advanceTime()` 与 `window.render_game_to_text()` 调试/自动化接口。

## 验证结果

执行 `npm test`：

| 测试 | 结果 |
| --- | ---: |
| 阶段3 | 63 / 63 |
| 阶段4 | 76 / 76 |
| 阶段5 | 99 / 99 |
| 阶段6 | 116 / 116 |
| 阶段7 | 65 / 65 |
| 合计 | **419 / 419** |

浏览器验证：`npm start` 后使用 Playwright 自动化客户端运行 3 轮，状态文本和 Canvas 截图生成成功，无页面错误日志；另点击「科研」分页验证页面可打开、无运行时错误。验证截图位于 `output/web-game-stage7-verified/` 与 `output/web-game-research-verified/`。

## 阶段7边界

本阶段按要求停止于技术实验室与科研系统；单位升级、装备、新战区、随机事件、手动战斗指挥均保留为后续阶段，不在本交付中实现。
