import fs from 'node:fs/promises';
import path from 'node:path';
import crypto from 'node:crypto';
import { fileURLToPath } from 'node:url';
import { CdpClient, getJson, listenEphemeral, localStaticServer, waitForWebSocket } from './cdp-client.mjs';
import { launchManagedBrowser, terminateManagedBrowser } from './managed-browser-process.mjs';
import { buildIsolatedTempEnv, createIsolatedTempRoot, removeIsolatedTempRoot } from './isolated-temp-root.mjs';

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '../..'); const machinePath = path.join(root, 'stage8_2g_da_machine_semantic_evidence.json'); const manifestPath = path.join(root, 'stage8_2g_da_browser_capture_manifest.json'); const screenshotDir = path.join(root, 'screenshots/stage8-2G-D-A'); const sha = (value) => crypto.createHash('sha256').update(value).digest('hex'); const call = (method, ...args) => `window.__IRON_COMMAND__[${JSON.stringify(method)}](${args.map((value) => JSON.stringify(value)).join(',')})`; const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
async function main() {
  const machine = JSON.parse(await fs.readFile(machinePath, 'utf8')); if (machine.stage !== '8.2G-D-A' || machine.frameCount !== 12) throw new Error('D-A machine evidence target list invalid'); await fs.rm(manifestPath, { force: true }); await fs.rm(screenshotDir, { recursive: true, force: true }); await fs.mkdir(screenshotDir, { recursive: true }); const isolatedRoot = createIsolatedTempRoot('iron-command-stage8G-DA-'); const evidenceEnv = buildIsolatedTempEnv(isolatedRoot); const pageErrors = []; const consoleErrors = []; const hashes = new Set(); let server; let browser; let cdp;
  try {
    server = localStaticServer(root); const port = await listenEphemeral(server); browser = await launchManagedBrowser({ url: 'about:blank', env: evidenceEnv, tempDir: path.join(isolatedRoot, 'browser-profiles') }); const targets = await getJson(`http://127.0.0.1:${browser.devtools.devtoolsPort}/json`); const pageTarget = targets.find((target) => target.type === 'page'); cdp = new CdpClient(await waitForWebSocket(pageTarget.webSocketDebuggerUrl)); cdp.on('Runtime.exceptionThrown', ({ exceptionDetails }) => pageErrors.push(exceptionDetails?.exception?.description || exceptionDetails?.text || 'page exception')); cdp.on('Runtime.consoleAPICalled', ({ type, args }) => { if (type === 'error') consoleErrors.push((args || []).map((arg) => arg.value ?? arg.description ?? '').join(' ')); }); await cdp.send('Page.enable'); await cdp.send('Runtime.enable'); await cdp.send('Log.enable'); await cdp.send('Page.navigate', { url: `http://127.0.0.1:${port}/` }); await cdp.evaluate(`new Promise((resolve, reject) => { const start = performance.now(); const poll = () => window.__IRON_COMMAND__ ? resolve(true) : performance.now() - start > 10000 ? reject(new Error('D-A page bootstrap timeout')) : setTimeout(poll, 25); poll(); })`);
    const viewport = () => cdp.evaluate(`(() => { const rect = document.querySelector('#base-canvas')?.getBoundingClientRect(); return { innerWidth, innerHeight, canvas: rect ? { width: rect.width, height: rect.height } : null }; })()`); const loaded = await cdp.evaluate(call('loadEvidenceBattle', { id: machine.scene.sceneId, seed: machine.scene.seed, report: machine.scene.sourceReport })); if (!loaded?.ok) throw new Error('D-A art showcase load failed'); await cdp.evaluate(call('setSpeed', 0)); const capturedFrames = [];
    for (const frame of machine.scene.frames) {
      await cdp.send('Emulation.setDeviceMetricsOverride', { width: frame.viewportKind === 'narrow' ? 780 : 1440, height: 900, deviceScaleFactor: 1, mobile: false }); if (frame.fallbackExpected) { const resetLoaded = await cdp.evaluate(call('loadEvidenceBattle', { id: machine.scene.sceneId, seed: machine.scene.seed, report: machine.scene.sourceReport })); if (!resetLoaded?.ok) throw new Error(`D-A fallback reload failed ${frame.file}`); await cdp.evaluate(call('setSpeed', 0)); } await cdp.evaluate(call('battlePresentationSetAssetDisabled', 'unit_friendly_infantry', frame.fallbackExpected)); await cdp.evaluate(call('battlePresentationRenderAt', frame.visualTimeSeconds)); const assetRuntime = frame.fallbackExpected ? await cdp.evaluate(`window.__IRON_COMMAND__.battlePresentationAssetStatus?.()`) : await cdp.evaluate(`new Promise((resolve) => { const start = performance.now(); const poll = () => { const state = window.__IRON_COMMAND__.battlePresentationAssetStatus?.(); if (state?.allReady || performance.now() - start > 3000) resolve(state); else setTimeout(poll, 50); }; poll(); })`); await sleep(70); const evidence = await cdp.evaluate(call('battlePresentationEvidenceStateAt', frame.visualTimeSeconds, { sceneId: machine.scene.sceneId, seed: machine.scene.seed, semanticName: frame.semantic })); if (!evidence?.ok) throw new Error(`D-A evidence state unavailable ${frame.file}`); await cdp.evaluate(call('battlePresentationRenderAt', frame.visualTimeSeconds)); const screenMetrics = await cdp.evaluate(call('battlePresentationScreenMetricsAt', frame.visualTimeSeconds)); if (!screenMetrics) throw new Error(`D-A runtime geometry unavailable ${frame.file}`); const pngPath = path.join(screenshotDir, frame.file); await cdp.screenshot(pngPath); const imageSha256 = sha(await fs.readFile(pngPath)); if (hashes.has(imageSha256)) throw new Error(`duplicate D-A PNG ${frame.file}`); hashes.add(imageSha256); const rows = screenMetrics.actors || []; if (rows.some((row) => row.rendererGeometrySource !== 'production-final-draw-geometry')) throw new Error(`D-A non-production geometry ${frame.file}`); if (frame.viewportKind === 'narrow' && rows.some((row) => row.screenFootprint < row.minimumScreenFootprint)) throw new Error(`D-A narrow footprint ${frame.file}`); if (frame.fallbackExpected) { const fallback = rows.find((row) => row.actorId === 'unit_stage8g-c1-mining-0'); if (!fallback || fallback.actualDrawPath !== 'procedural-fallback') throw new Error(`D-A fallback not visible ${frame.file}: ${JSON.stringify({ fallback, rows: rows.map((row) => row.actorId), assetRuntime })}`); } else if (!assetRuntime?.allReady) throw new Error(`D-A asset runtime not ready ${frame.file}`);
      capturedFrames.push({ ...frame, captureTimeMs: Number(evidence.state.time) * 1000, timestampDeltaMs: Number(evidence.state.time) * 1000 - frame.timeMs, viewport: await viewport(), stateSignature: evidence.c1StateSignature, environmentSignature: evidence.state.environment?.signature || null, destructionSignature: evidence.state.destruction?.signature || null, screenMetrics, assetRuntime: { allReady: assetRuntime?.allReady === true, assets: assetRuntime?.assets || [] }, imageSha256, screenshot: { path: path.relative(root, pngPath), sha256: imageSha256 }, browserStateSnapshot: evidence.c1Payload });
    }
    if (pageErrors.length || consoleErrors.length) throw new Error(`D-A browser errors ${JSON.stringify({ pageErrors, consoleErrors })}`); await fs.writeFile(manifestPath, `${JSON.stringify({ stage: '8.2G-D-A', version: 1, generatedBy: 'tests/browser/stage8-2G-D-A-evidence.mjs', independentAudit: false, machineEvidenceFile: path.basename(machinePath), machineEvidenceSha256: sha(await fs.readFile(machinePath)), browser: { currentCodeCaptured: true, legacyFallbackUsed: false, pageErrors, consoleErrors, screenshotsDir: path.relative(root, screenshotDir), captureCount: capturedFrames.length }, scenes: [{ sceneId: machine.scene.sceneId, result: machine.scene.result, seed: machine.scene.seed, sceneHash: machine.scene.sceneHash, frames: capturedFrames }], binding: { machineEvidenceToBrowserCapture: true, browserStateReadFromCurrentRenderer: true, finalDrawGeometryFromRenderer: true, animationResolverFromProductionRenderer: true, assetRuntimeStateFromRenderer: true, fallbackFrame: 'runtime-disabled-asset', exactTimestampToleranceMs: 16.7, duplicateImageHashes: false } }, null, 2)}\n`); console.log(JSON.stringify({ ok: true, stage: '8.2G-D-A', screenshots: capturedFrames.length, uniquePngHashes: hashes.size, pageErrors, consoleErrors, output: manifestPath }));
  } finally { cdp?.close(); await terminateManagedBrowser(browser).catch(() => {}); await new Promise((resolve) => server?.close(() => resolve())); removeIsolatedTempRoot(isolatedRoot); }
}
main().catch((error) => { console.error(JSON.stringify({ ok: false, code: 'stage8-2G-DA-browser-evidence-failed', message: error.message || String(error) })); console.error(error.stack || error); process.exitCode = 1; });
